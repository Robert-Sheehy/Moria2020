using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Damage : MonoBehaviour
{
    /* hit prob -> applied to weapon,item,STR
     * clac Crit chance -> between 1-5000
     * calc damage -> if crit hit run clac
     * bashing -> chance to stun
     * clac AC */

    //import fom Character stats


    //need these vars
    int weaponDamage = 1, itemDamage = 1;

    void Update()
    {
        
    }
}
